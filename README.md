# aws-lambda-events

Small TypeScript helpers to normalize AWS API Gateway proxy events (payload v1.0 and v2.0) for Lambda functions. The library exposes `RestApi.Request` and `HttpApi.Request` wrappers plus minimal response builders to keep handlers concise and predictable.
